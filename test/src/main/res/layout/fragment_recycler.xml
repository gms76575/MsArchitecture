<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:id="@+id/btnItemDecoration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:text="@string/test_item_decoration" />

    <Button
        android:id="@+id/btnRecyclerAdapter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/btnItemDecoration"
        app:layout_constraintStart_toStartOf="parent"
        android:text="@string/test_recycler_adapter" />

    <Button
        android:id="@+id/btnSingleAdapter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/btnRecyclerAdapter"
        app:layout_constraintStart_toStartOf="parent"
        android:text="@string/test_single_adapter" />

</androidx.constraintlayout.widget.ConstraintLayout>